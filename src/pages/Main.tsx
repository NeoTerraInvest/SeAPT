import { main as styles } from '@styles';
const Main = () => {
  return (
    <div id={styles.debug}>
      Home
      <span id={styles.comment}>Ready for Launch🚀</span>
    </div>
  );
};

export default Main;
