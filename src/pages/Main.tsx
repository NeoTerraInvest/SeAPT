import styles from '../styles/main.module.scss';
const Main = () => {
  return (
    <div id={styles.debug}>
      Home
      <span>Ready for Launch🚀</span>
    </div>
  );
};

export default Main;
